<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>{{productName}}</title>
  <link rel="stylesheet" href="styles.css" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
</head>
<body>
  <header class="header">
    <div class="container">
      <input type="text" id="p_id" value="{{p_id}}" hidden>
      <h1>{{p_Name}}</h1>
      <p>{{description}}</p>
      <button id="buyNowBtn">Order Now</button>
    </div>
  </header>

  <section class="product-section">
    <div class="product-info">
      <h2>{{title}}</h2>
      <ul>
        <li>✅ {{feature1}}</li>
        <li>✅ {{feature2}}</li>
        <li>✅ {{feature3}}</li>
      </ul>
      <div class="price">{{promoPrice}} DHs</div>
    </div>
    <div class="product-img">
      <img src="../../uploads/{{img1}}">
    </div>
    <div class="little_img">
      <ul>
        <li><img src="../../uploads/{{img1}}"></li>
        <li><img src="../../uploads/{{img2}}"></li>
        <li><img src="../../uploads/{{img3}}"></li>
      </ul>
    </div>
  </section>

  <footer class="footer">
    {{storeName}} &copy; 2025.
  </footer>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.bundle.min.js" integrity="sha384-j1CDi7MgGQ12Z7Qab0qlWQ/Qqz24Gc6BM0thvEMVjHnfYGF0rmFCozFSxQBxwHKO" crossorigin="anonymous"></script>
  <script src="./script.js"></script>
</body>
</html>
